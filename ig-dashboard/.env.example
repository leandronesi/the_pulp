# Turso — copia in .env e metti i tuoi valori.
# Se non set → fallback a SQLite locale in data/pulp.db.
#
# Da app.turso.tech → il tuo DB → Connect:
#   - Database URL (libsql://...)
#   - Auth Token (Create Token, scadenza Never)

TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
